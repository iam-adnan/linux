# DELIBERATE TEST FIXTURE — NOT A REAL CREDENTIAL.
#
# This file exists solely to verify that the CodeReviewer secret-scan workflow
# detects a leaked credential in a pull request. The value below is the same
# non-functional fixture used in the engine's own test suite
# (test/scan-org.test.js), so it has never been a live key and grants nothing.
#
# Expected result: the Secret Scan check goes red and a masked sticky comment
# appears on the PR, reporting rule `aws-access-token` at this file:line.
# If the check passes green, the scanner is NOT working.

[deployment]
region = us-east-1
aws_access_key_id = AKIAZX4J7Q2W9TB5V3EX
