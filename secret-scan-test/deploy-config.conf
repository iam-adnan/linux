# DELIBERATE TEST FIXTURE — NOT A REAL CREDENTIAL.
#
# Verifies that the secret-scan workflow flags a leaked credential in a PR.
# The value below is random and grants nothing.
#
# Expected: the Secret Scan check goes red and a masked sticky comment reports
# rule `generic-api-key` at this file:line. A green check means the scanner is
# NOT working.
#
# WHY NOT AN AWS KEY: the first attempt planted AKIAZX4J7Q2W9TB5V3EX (the value
# used in the engine's own test/scan-org.test.js) and the scan came back CLEAN.
# Verified locally against the pinned betterleaks 1.6.1: no `aws-access-token`
# rule fires for a bare AKIA-shaped key ID, on any path, with or without
# surrounding comments. The engine's test fixture is a hand-built finding object
# fed to test doubles, so it never exercises the real ruleset — it is not
# evidence that this scanner detects that shape.
#
# Shapes confirmed detected by betterleaks 1.6.1 in local probes:
#   private-key, github-pat, slack-bot-token, stripe-access-token,
#   generic-api-key
#
# generic-api-key is used here deliberately: it is not a provider pattern, so
# GitHub push protection ignores it and the repo's protection can stay ENABLED
# while the test runs.

[deployment]
region = us-east-1
api_key = 7f3d9a2e8b1c4f6a0d5e9c3b7a1f8d2e4c6b9a0f
