# Live-verification fixture for the secret-scan engine. NOT a real credential:
# a syntactically valid but randomly generated AWS key id, planted so the scanner
# has something to find. Deleted with the branch.
aws_access_key_id = AKIA4XZ7Q2W9TB5V3EXK
